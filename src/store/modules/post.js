import { createAction, handleActions}  from 'redux-actions';
import { Map } from 'immutable';
import { pender } from 'react-pender';

const initialState = Map({})

export default handleActions({

}, initialState);